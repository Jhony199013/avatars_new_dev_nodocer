{
  "name": "avatars_new_root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd front && npm run dev",
    "build": "cd front && npm run build",
    "start": "cd front && npm start",
    "lint": "cd front && npm run lint",
    "install": "cd front && npm install"
  }
}




















